# qlib-stock 环境变量配置示例
#
# 使用方法：
#   1. 复制本文件为 .env（或手动设置以下环境变量）
#   2. 根据实际路径修改右侧的值
#   3. 系统环境变量中设置同名变量也可生效（优先级：环境变量 > 配置文件）
#
# 注意：代码中优先读取环境变量，其次读取 config/settings.yaml 中的对应字段。
#       如果两者均未设置，相关功能会抛出 ValueError 明确报错，不会静默使用写死的默认路径。

# Qlib bin 格式数据根目录（必填）
# 对应 config/settings.yaml 中的 data_source.qlib_dir
QLIB_PROVIDER_URI=D:/trae/qlib_bin

# CSV 原始数据目录（AKShare 下载的数据存放位置，必填）
# 对应 config/settings.yaml 中的 data_source.csv_dir
CSV_DATA_DIR=D:/data

# MLflow 文件存储许可（可选，MLflow 2.x+ 与 Qlib Recorder 配合时需要）
# MLFLOW_ALLOW_FILE_STORE=true
